repositories {
    jcenter()
}
configurations {
    demo
}
def awsSdk = "1.11.19"

dependencies {
    demo ("com.amazonaws:aws-java-sdk-core:${awsSdk}") {
            exclude group: "commons-logging"
            exclude group: "com.fasterxml.jackson.core"
            exclude group: "org.apache.httpcomponents"
    }
    demo ("com.amazonaws:aws-java-sdk-s3:${awsSdk}") {
            exclude group: "commons-logging"
            exclude group: "com.fasterxml.jackson.core"
            exclude group: "org.apache.httpcomponents"
    }
    demo ("com.amazonaws:aws-java-sdk-s3:${awsSdk}") {
            exclude group: "commons-logging"
            exclude group: "com.fasterxml.jackson.core"
            exclude group: "org.apache.httpcomponents"
    }
    demo ("com.amazonaws:aws-java-sdk-cloudformation:${awsSdk}") {
            exclude group: "commons-logging"
            exclude group: "com.fasterxml.jackson.core"
            exclude group: "org.apache.httpcomponents"
    }
}

task printDemoDeps {doLast {
    configurations.demo.files.each {
        println it
    }
}}